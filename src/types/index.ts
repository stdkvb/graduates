export enum ApiConstants {
  API_BASE_URL = "https://support.wptt.ru/api",
  BASE_URL = "https://support.wptt.ru/",
}

export enum Emails {
  SUPPORT_EMAIL = "info@example.ru",
}

export enum StorageKeys {
  TOKEN = "TOKEN",
}
export enum FetchStatus {
  IDLE = "idle",
  LOADING = "loading",
  SUCCESS = "success",
  ERROR = "error",
}

export enum Messages {
  MISSING_DATA = "Нет данных",
}
